# Genism
This project focuses on building a Generative AI system using deep learning techniques to create new data samples that resemble a given dataset. The model learns underlying patterns and distributions to generate realistic outputs such as images, text, or other structured data.
